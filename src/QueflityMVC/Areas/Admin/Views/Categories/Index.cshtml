@inject IAuthorizationService AuthorizationService
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Mvc.TagHelpers
@using QueflityMVC.Application.Constants
@model QueflityMVC.Application.ViewModels.Category.ListCategoriesVm

@{
    ViewData["Title"] = "List Categories";
    int[] options = [2, 5, 10, 25, 50, 100];
    bool canCreateEntities = (await AuthorizationService.AuthorizeAsync(User, Policies.ENTITIES_CREATE)).Succeeded;
    bool canEditEntities = (await AuthorizationService.AuthorizeAsync(User, Policies.ENTITIES_EDIT)).Succeeded;
}

@section Styles{
    <link rel="stylesheet" href="/css/pagination.css"/>
}

<div class="container-fluid p-4">

    <h1 class="mb-4">
        <i class="fas fa-tags card-icon"></i>
        Categories
    </h1>

    @if (canCreateEntities)
    {
        <p class="mb-4">
            <a class="btn btn-primary" asp-action="Create" asp-controller="Categories" asp-area="Admin">
                <i class="fas fa-plus-circle"></i> Create New
            </a>
        </p>
    }

    <form asp-action="Index" asp-controller="Categories" asp-area="Admin" method="post" class="mb-4">
        <div class="row align-items-center mb-3">
            <!-- Search Input -->
            <div class="col-md-4 mb-2 mb-md-0">
                <div class="input-group">
                    <span class="input-group-text bg-primary text-white">
                        <i class="fas fa-search"></i>
                    </span>
                    <input type="text" class="form-control" placeholder="Search..." asp-for="NameFilter"
                           name="nameFilter" id="nameFilter"/>
                </div>
            </div>
            <!-- Search Button -->
            <div class="col-md-2 mb-2 mb-md-0">
                <button type="submit" class="btn btn-primary w-100">
                    <i class="fas fa-filter"></i> Search
                </button>
            </div>
            <!-- Page Size Selector -->
            <div class="col-md-3">
                <div class="input-group">
                    <label class="input-group-text bg-primary text-white" for="pageSizeSelect">
                        <i class="fas fa-list"></i>
                    </label>
                    <select class="form-select" asp-for="Pagination.Info.PageSize" id="pageSizeSelect"
                            onchange="PageSizeSelected()">
                        @foreach (int i in options)
                        {
                            if (i == Model.Pagination.Info.PageSize)
                            {
                                <option value="@i" selected>@i</option>
                            }
                            else
                            {
                                <option value="@i">@i</option>
                            }
                        }
                    </select>
                </div>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th class="text-center">Actions</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var item in Model.Pagination.Entities)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.Id)</td>
                        <td>@Html.DisplayFor(modelItem => item.Name)</td>
                        <td class="text-center">
                            <div class="btn-group" role="group" aria-label="Manage category">
                                @if (canEditEntities)
                                {
                                    <a class="btn btn-outline-secondary btn-sm" asp-action="Edit" asp-route-id="@item.Id">
                                        <i class="fas fa-edit"></i> Edit
                                    </a>
                                    <a class="btn btn-outline-primary btn-sm" asp-action="ViewItems" asp-route-id="@item.Id">
                                        <i class="fas fa-eye"></i> View Items
                                    </a>
                                }
                                @if (canCreateEntities)
                                {
                                    <a class="btn btn-outline-danger btn-sm" asp-action="Delete" asp-route-id="@item.Id">
                                        <i class="fas fa-trash-alt"></i> Delete
                                    </a>
                                }
                            </div>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>

        <div class="row">
            <partial name="_PaginationPartial" for="Pagination.Info"/>
        </div>
    </form>
</div>

@section Scripts
{
    <script>
    function PageSizeSelected() {
        SubmitPagination();
    }
</script>
}