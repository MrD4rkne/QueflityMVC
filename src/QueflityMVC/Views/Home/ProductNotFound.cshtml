@using Microsoft.AspNetCore.Mvc.TagHelpers
@model int

@{
    ViewData["Title"] = "Product not found";
}

<h1>Product was not found</h1>

<div>
    @Html.ActionLink("Retry", "Contact", "Home", new { id = Model }) |
    <a asp-action="Index">Back to Home</a>
</div>