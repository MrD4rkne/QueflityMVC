@using Microsoft.AspNetCore.Mvc.TagHelpers
@model QueflityMVC.Application.ViewModels.Other.FirstMessageInConversationVm

@{
ViewData["Title"] = "Contact";
}

@section Styles {
<style>
    .contact-container {
        flex: 1;
        display: flex;
        flex-wrap: wrap;
        margin: 20px auto;
        background-color: #f9f9f9;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        overflow: hidden;
    }

    .contact-left {
        flex: 1;
        min-width: 300px;
        padding: 20px;
        background-color: #ffffff;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: flex-start;
        border-right: 1px solid #e0e0e0;
    }

    .product-image {
        max-width: 100%;
        height: auto;
        border-radius: 5px;
        margin-bottom: 20px;
    }

    .product-name {
        font-size: 22px;
        font-weight: bold;
        color: #333333;
        margin-bottom: 10px;
    }

    .product-price {
        font-size: 18px;
        color: #666666;
        margin-bottom: 20px;
    }

    .contact-right {
        flex: 1;
        min-width: 300px;
        padding: 40px;
        background-color: #fafafa;
    }

    .contact-form {
        display: flex;
        flex-direction: column;
    }

    .contact-form h3 {
        margin-bottom: 20px;
        font-size: 24px;
        font-weight: bold;
        color: #333333;
    }

    .form-group {
        margin-bottom: 15px;
    }

    .form-group label {
        font-weight: 600;
        color: #555555;
    }

    .form-control {
        border: 1px solid #cccccc;
        border-radius: 5px;
        padding: 10px;
    }

    .btn-primary {
        background-color: #007bff;
        border: none;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
    }

    .btn-primary:hover {
        background-color: #0056b3;
    }
</style>
}

<div class="contact-container">

    <div class="contact-left">
        <img src="@Model.Product.Image.FileUrl" alt="@Model.Product.Image.AltDescription" class="product-image"/>
        <div class="product-name">@Model.Product.Name</div>
        <div class="product-price">$@Model.Product.Price</div>
    </div>

    <div class="contact-right">
        <form asp-action="Contact" asp-controller="Home" class="contact-form">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            @Html.HiddenFor(model => model.Product.Id)

            <h3>Contact Us!</h3>

            <div class="form-group">
                <label asp-for="Title" class="control-label"></label>
                <input type="text" class="form-control" asp-for="Title"/>
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Message" class="control-label"></label>
                <textarea class="form-control" asp-for="Message"></textarea>
                <span asp-validation-for="Message" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Send Message" class="btn btn-primary"/>
            </div>
        </form>
    </div>
</div>
