@model QueflityMVC.Application.ViewModels.User.ListUsersVM

@{
    ViewData["Title"] = "Users";
    int[] options = new int[] { 2, 5, 10, 25, 50, 100 };
}

<form asp-action="Index" asp-controller="Admin" method="post">
    <ul class="nav gap-1">
        <li class="nav-item">
           <input type="text" class="form-control" placeholder="Search Phrase" asp-for="UserNameFilter" />
        </li>
        <li class="nav-item">
            <button type="submit" class="btn btn-secondary">Search</button>
        </li>
        <li class="nav-item">
            <select class="form-select" asp-for="Pagination.Info.PageSize" onchange="javascript:PageSizeSelected()">
                @foreach (int i in options)
                {
                    @if (i == Model.Pagination.PageSize)
                    {
                        <option value="@i" selected>@i</option>
                    }
                    else
                    {
                        <option value="@i">@i</option>
                    }
                }
            </select>
        </li>
    </ul>
    <div class="row">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Id
                    </th>
                    <th>
                        Username
                    </th>
                    <th>
                        Email
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Pagination.Entities)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.UserName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Email)
                        </td>
                        <td>
                            @Html.ActionLink("Roles", "ManageUserRoles", new { id=item.Id }, new { @class = "btn btn-outline-info btn-sm"})
                            @Html.ActionLink("Claims", "ManageUserClaims", new { id=item.Id }, new { @class = "btn btn-outline-success btn-sm"})
                            @Html.ActionLink("Delete", "DeleteUser", new { id=item.Id }, new { @class = "btn btn-outline-danger btn-sm"})
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="row">
        <partial name="_PaginationPartial" for="Pagination.Info" />
    </div>
</form>

@section Scripts
    {
    <script type="text/javascript">
        function PageSizeSelected() {
            document.forms[0].submit();
        }
    </script>
}